import 'package:flutter/material.dart';

class MainMenuRouteBuilder extends PageRouteBuilder {
  MainMenuRouteBuilder({@required WidgetBuilder pageBuilder})
      : super(pageBuilder: (context, _, __) => pageBuilder(context));
}
